---
- name: Set hostname
  ansible.builtin.hostname:
    name: "{{ hostname }}"
    use: openbsd
  when: hostname

- name: Configure doas
  template:
    src: doas.conf.j2
    dest: /etc/doas.conf
    mode: "0644"
